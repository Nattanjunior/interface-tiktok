import { View,StyleSheet,Text } from 'react-native';

export default function Search() {
 return (
   <View style={styles.container}>
     <Text>Página de buscas</Text> 
   </View>
  );
}

const styles = StyleSheet.create({
    container:{
        flex:1
    }
})